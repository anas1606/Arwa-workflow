{
  "name": "arwa-workflow",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @arwa/web",
    "build": "npm run build -w @arwa/web"
  },
  "engines": {
    "node": ">=20"
  }
}
